import React from 'react'

export default class AppBar extends React.Component {
    render() {
        return (
            <nav className="navbar">
                
            </nav>
        )
    }
}